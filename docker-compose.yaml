services:
  api:
    build: .
    container_name: whatsapp-transcriber-api
    ports:
      - "8000:8000"
    volumes:
      - ./input_audios:/app/input_audios
      - ./transcriptions:/app/transcriptions
    restart: unless-stopped